@model IEnumerable<gestioncommande.Models.Produit>

    <h2>Liste des Produits</h2>

    <table class="table">
        <thead>
            <tr>
                <th>Libell�</th>
                <th>Quantit� en Stock</th>
                <th>Prix Unitaire</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var produit in Model)
        {
            <tr>
                <td>@produit.Libelle</td>
                <td>@produit.QuantiteStock</td>
                <td>@produit.PrixUnitaire</td>
                <td>
                    <a href="@Url.Action("Edit", "Produit", new { id = produit.Id })">Modifier</a>
                    <a href="@Url.Action("Details", "Produit", new { id = produit.Id })">D�tails</a>
                </td>
            </tr>
        }
        </tbody>
    </table>
